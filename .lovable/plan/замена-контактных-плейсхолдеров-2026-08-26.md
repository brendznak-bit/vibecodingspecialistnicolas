# Замена контактных плейсхолдеров

## Цель
Заменить placeholder-контакты в секции «Контакты» и футере на реальные данные пользователя.

## Что изменится
- В `src/components/landing/ContactCta.tsx` обновить константы:
  - `TELEGRAM_URL` → `https://t.me/@nikolyavasili`
  - `EMAIL` → `brendznak@gmail.com`
- `src/components/landing/SiteFooter.tsx` импортирует эти же константы, поэтому футер обновится автоматически.
- Никакие другие файлы и секции затронуты не будут.

## Проверка
- Сборка проекта (`build:dev` / логи сборки) должна пройти без ошибок.
- В превью секция «Контакты» и футер должны показывать новые email и ссылку Telegram.
